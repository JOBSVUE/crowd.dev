# macOS
.DS_Store

# VCS & build system files
.git
.gitignore
Dockerfile
docker-compose*.yml

# Node artifacts
node_modules
dist
build
coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# E2E / test stuff (skip if you actually need these in the image)
cypress
.cypress-cache

# Env & secrets (keep examples)
.env*
!.env.example
!.env.dist

# Python cruft (harmless if present; useful in mixed repos)
__pycache__/
**/__pycache__**
*.py[cod]
*.pyc
